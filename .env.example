# 复制本文件为项目根 ``.env`` 后按需填写（extract_pdf 等会通过 step_dotenv 加载）。
# 与 pipeline.json 同名键冲突时：环境变量覆盖 JSON（见 pipeline.json 顶部 __readme）。

# ----- OpenDataLoader：云端 Docling Hybrid（force OCR 须在云上启动 hybrid 时加 --force-ocr）-----
# 根 URL，须含协议与端口，勿尾斜杠。使用 SSH 本地转发时可不设此项并保持 pipeline.json 中 hybrid_url 为 http://127.0.0.1:5002
# OPENDATALOADER_HYBRID_URL=http://你的云服务器公网IP或域名:5002

# 可选：跨公网时加大 /health 探测超时（秒）；也可只在 pipeline.json 里调 hybrid_health_timeout_sec
# OPENDATALOADER_HYBRID_HEALTH_TIMEOUT_SEC=30
